<?php
/**
 * The DeleteRequestable interface specifies functions that are required for 
 * DELETE Method calls of any media type.
 * 
 * @author eugene
 */
interface DELETERequestable extends Requestable
{
    // Thus far empty, DELETE Requestable uses only default functionality that
    // any Requestable object should have. Kept here to be consistent with other
    // Requestable types.
}
